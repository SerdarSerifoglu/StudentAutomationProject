@model List<StudentAutomationProject.Models.ExamResult.ExamResultViewModel>

@{
    ViewData["Title"] = "Sınav Sonucu Gir";
    Layout = "~/Views/_StudentAffairsLayout.cshtml";
}
<style>
    .form-control{
        border:0!important;
    }
</style>

@*<form asp-controller="ExamResult" asp-action="Add" method="post">
    @for (int i = 0; i < Model.Count(); i++)
    {
    <div class="form-control">
        <label>@Model[i].PersonName</label>
        <input asp-for="@Model[i].Grade" />
        <input asp-for="@Model[i].PersonUID" type="hidden" />
        <input asp-for="@Model[i].ExamUID" type="hidden" />
    </div>
    }
    <br />
    <input class="btn btn-success" type="submit" value="Notları Kaydet" />
</form>*@

<table class="table">
    <thead>
        <tr>
            <th>
                İsim
            </th>
            <th>
                Soyisim
            </th>
            <th>
                Not
            </th>
            <th>

            </th>
            <th></th>
        </tr>
    </thead>
    <form asp-controller="ExamResult" asp-action="Add" method="post">
        <tbody>
            @for (int i = 0; i < Model.Count(); i++)
            {
            <tr>
                <td>
                    @Model[i].PersonName
                </td>
                <td>
                    @Model[i].PersonLastName
                </td>
                <td>
                    <input asp-for="@Model[i].Grade" />
                </td>
                <input asp-for="@Model[i].PersonUID" type="hidden" />
                <input asp-for="@Model[i].ExamUID" type="hidden" />
            </tr>
            }
        </tbody>
        <tfoot>
            <tr>
                <td><input class="btn btn-success" type="submit" value="Notları Kaydet" /></td>
            </tr>
        </tfoot>
    </form>
</table>
